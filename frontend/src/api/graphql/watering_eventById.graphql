query QueryWateringEventById($id: UUID!) {
  wateringEvent(wateringEventId: $id) {
    id
    plant {
      id
      name
      location {
        id
        name
      }
    }
    person {
      id
      name
    }
    ctime
    notes
  }
}
