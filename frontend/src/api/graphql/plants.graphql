query QueryPlants {
  plants {
    id
    name
    birthday
    image
    wateringFrequency
    wateringInstructions
    lastWateredDate
    lastWateredBy {
      id
      name
    }
    location {
      id
      name
    }
  }
}
